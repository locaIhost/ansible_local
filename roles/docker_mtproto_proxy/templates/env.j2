{% if tgmptp_key | default(None) is not none and tgmptp_key | string | trim | length > 0 %}
TG_KEY={{ tgmptp_key }}
{% endif %}
{% if tgmptp_tag | default(None) is not none and tgmptp_tag | string | trim | length > 0 %}
TG_TAG={{ tgmptp_tag }}
{% endif %}